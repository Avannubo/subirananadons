// Utility to populate user(s) if needed. For now, returns input unchanged.
export async function populateUser(query) {
    // If you need to populate fields, add logic here (e.g., .populate('field'))
    return await query;
}
